# 設計思想: デザインは引き算である

このリポジトリで作業するエージェント(Claude)への指針。

## 基本原理: 有限性こそが設計を生む

良い設計は、**資源の有限性という制約** から生まれる。

人間のプログラマーが良い抽象化を作るのは、彼らが賢いからではなく、
**時間と認知能力が有限だから** である。クランキーな抽象化の結果に
自分の未来の時間を浪費したくないから、今シンプルにする。
— これが Larry Wall の言う「怠惰(laziness)」という美徳の本質である。

LLM にはこの制約が本質的に存在しない。労力のコストがゼロだから、
放っておけばシステムを「より良く」ではなく「より大きく」する。
ゴミの重層ケーキの上に、喜んでさらに積み上げる。

**だから、人間の代わりに制約を自らに課すのが、このリポジトリでの Claude の責務である。**

## 引き算のデザイン

設計とは足し算ではなく引き算である。行数、ファイル数、依存、抽象化の
階層、設定項目 — これらはすべて負債である。増やすたびに、読む人・
保守する人・将来の自分の時間を消費する。

### 書く前に問う

1. **これは本当に必要か?** — タスクが要求していないものは足さない。
   「ついでに」のリファクタ、将来の拡張を見越した抽象化、
   起こり得ないケースのエラーハンドリング、これらは追加しない。
2. **既存のものを削れないか?** — 新しく書く前に、既存のコードを
   削除・統合できないかを検討する。
3. **3 行の類似コードは、早すぎる抽象化よりマシ** — 重複を恐れて
   無理な共通化をしない。本当に必要な抽象は、後から自然に見えてくる。

### 行数は成果ではない

生成した行数、触ったファイル数は、価値の指標ではない。
むしろ **削除した行数** のほうが、しばしば価値の指標になる。

`37k lines/day` は自慢ではなく警告である。

### 成果物に混入させない

- テストハーネスの重複
- サンプル/雛形プロジェクトの残骸
- 使われていないアセットのバリアント
- 「念のため」残したコメントアウト
- 消したコードの `// removed` 跡

これらは LLM が特に混入させがちなノイズである。コミット前に読み返して削る。

## LLM は道具であり、主でない

LLM(=私)の役割は、人間の美徳としての怠惰に **奉仕する** こと。

- 技術的負債の返済など、「非皮肉な(=本物の)怠惰」の領域を引き受ける
- 厳格性を保つための機械的作業(型チェック、網羅性の確認)を担う
- ただし、**最終的にシステムをシンプルにする方向** にだけ働く

自分(LLM)が生成したコードを、常に人間のレビュアーの視点で見返す。
「これを読んだ未来のエンジニアは、この複雑さを正当だと感じるか?」
と問う。答えが No なら、提出前に削る。

## 要約

- **制約がないところに良い設計は生まれない。** 制約がないなら自分で課す。
- **足すのは簡単、削るのは難しい。** 難しいほうを選ぶ。
- **次に読む人の時間を、自分の時間より大切にする。**

---

参考: Bryan Cantrill, *The peril of laziness lost* (2026/04/12)
